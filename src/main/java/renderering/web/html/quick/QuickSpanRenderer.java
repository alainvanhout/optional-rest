package renderering.web.html.quick;

import renderering.web.html.basic.documentbody.SpanRenderer;

public class QuickSpanRenderer extends SpanRenderer implements QuickElement {
    public QuickSpanRenderer() {
    }

    public QuickSpanRenderer(Object item) {
        super(item);
    }
}
